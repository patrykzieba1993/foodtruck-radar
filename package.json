{
  "name": "foodtruck-radar-api",
  "version": "1.0.0",
  "description": "Rest API for foodtruck radar app",
  "scripts": {
    "dev": "nodemon index.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "start": "node index.js",
    "test": "export NODE_ENV=test CACHE_ENABLED=false && npm run lint:fix && mocha --recursive",
    "test:nolint": "export NODE_ENV=test CACHE_ENABLED=false && mocha --recursive"
  },
  "engines": {
    "node": "8.9.3"
  },
  "author": "patrykzieba1993",
  "license": "UNLICENSED",
  "dependencies": {
    "eslint-plugin-import": "^2.12.0",
    "eslint-plugin-mocha": "^5.0.0",
    "eslint-plugin-security": "^1.4.0",
    "nodemon": "^1.17.5"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-airbnb": "^16.1.0",
    "mocha": "^5.2.0"
  }
}
